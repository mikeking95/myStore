setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release:
  imaeg: web
  command:
    - python manage.py collectstatic --noinput
run:
  web: gunicorn mystore_project.wsgi
